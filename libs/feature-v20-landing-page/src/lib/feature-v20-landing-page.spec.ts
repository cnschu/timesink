import { featureV20LandingPage } from './feature-v20-landing-page';

describe('featureV20LandingPage', () => {
  it('should work', () => {
    expect(featureV20LandingPage()).toEqual('feature-v20-landing-page');
  });
});
